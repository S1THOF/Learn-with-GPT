const user = {
    username: 'Carlo',
    city: 'Askaban',
    favoriteNumber: 228,
    sex: 'Male',
  }
  const { username, city, favoriteNumber, sex } = user;
  
  console.log(username, city, favoriteNumber, sex);