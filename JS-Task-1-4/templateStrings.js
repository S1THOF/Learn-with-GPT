const helloUserInfo = ({username, city, favoriteNumber, sex}) => {
    console.log(`Hello, ${username}! You are from ${city}, right? Your favorite number is ${favoriteNumber} and you ${sex}.`)
  };
  
  helloUserInfo(user)